interface:
  display_name: "Model Orchestrator"
  short_description: "Split work visibly across cheaper and stronger model lanes"
  default_prompt: "Use $model-orchestrator only if this task can be decomposed into visible subtasks with real delegation and token savings. Show the delegation ledger, route bounded work to cheaper lanes, and keep final judgment in the orchestrator."
